---
import { features } from "../lib/utils"; 
---

<section
  class="flex flex-col items-center justify-center gap-2 py-6 sm:gap-4 sm:py-12 md:gap-6 md:py-24"
>
  <div class="h-[2px] w-[20%] self-center bg-green-300 md:self-start"></div>

  <div
    class="grid grid-cols-1 items-center gap-2 py-8 md:py-4 md:grid-cols-2 md:gap-3"
  >
    <h2
      class="text-center text-2xl font-semibold text-white sm:text-3xl md:text-left md:text-4xl"
    >
      Fitur yang dirancang dengan mempertimbangkan acara Anda
    </h2>
    <p class="text-center text-xs text-green-300 sm:text-base md:text-end">
      Dari pemesanan tempat hingga umpan balik pasca-acara, Flow memiliki semua
      yang Anda perlukan untuk menyederhanakan perencanaan acara Anda.
    </p>
  </div>

  <section
    class="grid w-full grid-cols-1 gap-2 text-white *:min-h-[550px] md:grid-cols-2 md:gap-4"
  >
    {
      features.map((feature, i) => {
        const colSpan = i === 1 || i === 2 ? "md:col-span-1" : "md:col-span-2";
        const imageCenter = i === 2 ? "" : "mx-auto";
        return (
          <article
            class={`relative w-full rounded-xl bg-white/10 p-4 sm:p-8 md:rounded-2xl md:p-6 ${colSpan}`}
          >
            <h4 class="text-xl font-semibold sm:text-2xl md:text-3xl">
              {feature.title}
            </h4>
            <p class="mt-2 md:mt-4">{feature.description}</p>
            <img
              src={feature.img}
              alt={`feature-${i + 1}`}
              class={`absolute right-0 bottom-0 left-0 h-[350px] rounded-b-xl object-cover sm:h-[400px] md:h-3/4 md:rounded-t-2xl ${imageCenter}`}
            />
          </article>
        );
      })
    }
  </section>
</section>

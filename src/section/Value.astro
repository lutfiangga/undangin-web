---
import { values } from "../lib/utils";
---

<section
  class="flex w-full flex-col items-center gap-2 md:flex-row md:justify-between md:gap-4"
>
  <!-- Image Container -->
  <div
    class="relative mx-6 h-[400px] w-full rounded-lg border border-t-green-300 border-r-green-300 border-l-green-300 bg-gradient-to-t from-green-300 to-black sm:rounded-xl md:h-[550px] md:w-1/2 md:rounded-2xl"
  >
    <img
      src="/images/value.webp"
      alt="value"
      class="absolute right-0 bottom-0 left-0 mx-auto max-w-3/4 rounded-t-xl md:rounded-t-2xl"
    />
  </div>

  <!-- Text & Values List -->
  <div
    class="flex w-full flex-col gap-3 p-4 text-white md:w-1/2 md:rounded-2xl"
  >
    <div class="h-[2px] w-[10%] bg-green-300"></div>

    <h2 class="text-2xl font-semibold sm:text-3xl md:text-4xl">
      Buat Acara Anda Menjadi Menyenangkan dengan Mudah
    </h2>

    <p>
      Izinkan kami memandu Anda melalui langkah cepat dan mudah untuk mewujudkan
      acara impian Anda.
    </p>

    {
      values.map((value, i) => (
        <article class="flex cursor-pointer items-center gap-2 p-2 hover:rounded-xl hover:bg-green-300/20 md:gap-4">
          <button class="cursor-pointer rounded-md bg-white/20 px-4 py-3 font-bold md:rounded-xl md:px-6 md:py-4">
            {i + 1}
          </button>

          <section class="flex flex-col gap-2">
            <h5 class="text-xl font-semibold">{value.title}</h5>
            <p>{value.description}</p>
          </section>
        </article>
      ))
    }
  </div>
</section>
